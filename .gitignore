# Ignore ONNX models (contact admin for model files)
captcha-model/

# ONNX Runtime Web files (download separately)
ort*.js
ort*.mjs
ort*.wasm

# TensorFlow.js (deprecated)
tf*.js

# Node modules
node_modules/

# Keras models (not needed for runtime)
*.keras
